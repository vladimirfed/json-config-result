function Description() {
  return (
    <div className="App container">
      This app allows to generate dynamic form from JSON with React. In section
      Config you can add a JSON template. In section Result you can see the
      dynamically created forms. JSON template example you can find in
      'formElement.json'
    </div>
  );
}

export default Description;
